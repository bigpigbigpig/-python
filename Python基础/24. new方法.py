# 初始化使用 init# 为对象分配空间，类的对象只有一份# 在内存中为对象分配空间，将对象的引用作为返回值给解释器，解释器将引用给init的第一个参数，进行初始化# new方法必须返回对象的引用给解释器class MusicPlayer(object):    instance = None    flag = False    @classmethod    def __new__(cls, *args, **kwargs):        if cls.instance is None:            print('i can run by myself')            cls.instance = super().__new__(cls)        return cls.instance    # def __init__(self):    #     print('player initial %s' )    def __init__(self, name):        if MusicPlayer.flag is True:            return        MusicPlayer.flag = True        self.name = name        print('player initial %s' % self.name)player1 = MusicPlayer('Ryan')print(player1)player2 = MusicPlayer('Evan')print(player2)player3 = MusicPlayer('Eva')print(player3)