# 可以使用__name__的神仙操作来解决此类事情# __name__ 开发一个模块时，测试模块能够正常执行# __name__# 被其他模块导入时，就是__name__的模块名# 模块测试的代码，在执行模块的时候才会执行# 主程序中，引用的是模块的名称# 测试模块的标准写法def main():    print("test function")if __name__ == "__main__":    main()