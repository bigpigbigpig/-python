# !/usr/bin/env python# -- coding:utf-8 --# 小猫爱吃鱼，小猫要喝水class Cat:    '''cat link eat fish.    cat need drink water.    '''    def eat(self):        print("%s like eat fish." % self.name)    def drink(self):        print("cat need drink water.")    def play(what):        print("%s play what" % what)tom = Cat()# tom.name = "Tom"tom.eat()tom.name = "Tom"# 在类的外部给类增加属性，不推荐# 在类的外部给类增加属性，不推荐# 在类的外部给类增加属性，不推荐# 在类的外部给类增加属性，不推荐# 在类的外部给类增加属性，不推荐